export const enum Type {
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
  DEFAULT = 'default'
}

export const enum Default {
  COLLAPSE_DURATION = 0,
  CSS_NAMESPACE = 'Toastify',
  CONTAINER_ID = 1
}

export const enum Direction {
  X = 'x',
  Y = 'y'
}
